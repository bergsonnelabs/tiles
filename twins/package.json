{
  "name": "@bergsonnelabs/tile-twins",
  "version": "0.3.0",
  "private": true,
  "description": "Tile digital twins: each tile's behavior, power and controls, next to its driver. Consumed by Studio and the portal.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9",
    "@types/node": "^22.0.0"
  }
}
